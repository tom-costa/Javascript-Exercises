//Guest List Checker:

var guestList = ["Tom", "Nick", "Bob"];

var guestName = prompt("What is your name?");

if (guestName.includes(guestName)) {
    alert(guestName + "is here");
} else {
    alert("not this time.");
}
